import { ErrorImagePipe } from './error-image.pipe';

describe('ErrorImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ErrorImagePipe();
    expect(pipe).toBeTruthy();
  });
});
